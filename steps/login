module.exports = {

  login(I, usuario, senha) {

    I.waitForElement('#username', 15);

    if (usuario) {
      I.fillField('#username', usuario);
    }

    if (senha) {
      I.fillField('#password', senha);
    }

    I.click('#login_button');

  }

};